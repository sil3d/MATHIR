# 📊 MATHIR — Results Index

**All benchmark/stress-test/training output JSONs live here.**

> ⚠️ **WARNING — OLD RESULTS ARE SUPERSEDED.** Files in this directory (e.g. `compare_all_approaches.json`) contain the **old fake "keyword-overlap" metrics** (e.g. "45.7% quality"). These are **NOT real IR benchmarks**. See V7.5 changelog entry for the correct real BEIR numbers. The authoritative benchmark files are now in `benchmarks/`:
> - `benchmarks/multi_dataset_efficient_results.json` — Real BEIR SciFact + NFCorpus
> - `benchmarks/controlled_results.json` — Controlled 5-system comparison on SciFact
> - `benchmarks/BEIR_BENCHMARK_REPORT.html` — Full HTML report

---

Before V7.4 these were scattered across the project root. They are now
consolidated in a single directory with a stable, descriptive filename
scheme. The original `*_results.json` files at the project root are
deprecated; the benchmark scripts in `benchmarks/` now write here.

---

## 📁 Files in this directory

| File | What it contains | Generated by |
|------|------------------|--------------|
| **`compare_all_approaches.json`** | Side-by-side comparison of FAISS / MATHIR V7 / A / B / C / D on the Fluid Mechanics textbook (200 chunks, 50 queries, 384-dim). Quality, latency, throughput, hits. | `python benchmarks/compare_all_approaches.py` |
| **`approach_d_vs_faiss.json`** | Head-to-head: FAISS vs MATHIR Approach D (BM25 + cross-encoder re-rank) on the same textbook. | `python benchmarks/approach_d_vs_faiss.py` |
| **`book_stress_test.json`** | VectorDB vs MATHIR (raw 256-dim embeddings) on the textbook. Latency, throughput. | `python benchmarks/book_stress_test.py` |
| **`book_stress_test_real_emb.json`** | Same as above but with real `all-MiniLM-L6-v2` sentence-transformer embeddings. Adds top-1 overlap and hit-count quality metrics. | `python benchmarks/book_stress_test_real_emb.py` |
| **`real_stress_test.json`** | "Original vs Cache-only vs Cache+Adaptive" latency/quality on 50 mixed queries (repeat / similar / novel). Demonstrates the 5-12× warm-path speedup. | `python benchmarks/real_stress_test.py` |
| **`stress_cache_warm.json`** | Four real-world scenarios × {Original, Cache, Cache+Adaptive}. Shows the 80-85% cache hit rate and the latency/quality trade-off. | `python benchmarks/stress_cache_warm.py` |
| **`latency_optimization.json`** | Approach-D variants: Cache off/Adaptive off, Cache only, Adaptive only, Cache+Adaptive. Cold vs warm latency + warmup speedup. | `python benchmarks/optimization_comparison.py` |
| **`v6_vs_v7.json`** | V6 vs V7 on compression, inference latency, model size, recall availability, anomaly accuracy, router collapse. | `python benchmarks/v6_vs_v7.py` |
| **`capacity_log.json`** | MATHIR retention scores at steps 10k / 20k / 30k (latest run). | `python benchmarks/book_stress_test.py` (legacy field) |
| **`capacity_log_V1_V2.json`** | Older V1/V2 retention run (210 000 steps). Kept for the long-horizon claim. | legacy v1/v2 trainer |
| **`benchmark_results.json`** | The original V5 suite (Personal Info / Project Knowledge / Long Sequential) — MATHIR vs RAG vs VectorDB. 600+ lines of detail. | `python benchmarks/compare_all_approaches.py` (early version) |
| **`mathir_best_params.json`** | Best hyper-parameters found by the V5 evolution trainer (retention decay, hidden dim, score, step). | legacy_v1_v3 evolution trainer |
| **`optimization.log`** | Plain-text optimization trace from `optimize_mathir.py` (if it ran). | legacy v1/v3 optimizer |

---

## 🔗 How to read them

- All JSONs are flat (no nested metadata) and are safe to load with
  `json.load(open(...))` — see the schemas in each script that produces
  them.
- `compare_all_approaches.json` is the canonical V7.1 retrieval-quality
  report — start there.
- `book_stress_test_real_emb.json` is the most realistic real-embedding
  benchmark (uses MiniLM).
- `v6_vs_v7.json` is the version-comparison report for the V7 release
  notes.

---

## 🛠️ Re-generating the results

Each benchmark script in `benchmarks/` writes its own JSON to this
directory. To re-run them all:

```bash
python benchmarks/compare_all_approaches.py
python benchmarks/approach_d_vs_faiss.py
python benchmarks/book_stress_test.py
python benchmarks/book_stress_test_real_emb.py
python benchmarks/real_stress_test.py
python benchmarks/stress_cache_warm.py
python benchmarks/optimization_comparison.py
python benchmarks/v6_vs_v7.py
```

Output paths are hard-coded in each script — see the `os.path.join(...)`
call near the end of each one.

---

*Last reorganized: V7.4 (2026-06-03). BEIR benchmarks added V7.5 (2026-06-05).*
