# DEPRECATED — All env vars are now centralized at benchmarks/.env (root)
#
# This file is kept for backward compatibility with scripts that auto-detect
# .env in this directory. It just points to the centralized config.
#
# To configure benchmarks:
#   1. Copy benchmarks/.env.example to benchmarks/.env
#   2. Fill in your API keys
#   3. (No per-directory .env needed)
#
# See: benchmarks/_env.py for the shared loader
